services:
  - type: web
    name: edos-dashboard
    env: node
    buildCommand: npm run build && npm run seed --prefix server
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
